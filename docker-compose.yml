services:
  arthabook-api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: arthabook-api

    ports:
      - "8080:8080"

    env_file:
      - .env

    restart: unless-stopped